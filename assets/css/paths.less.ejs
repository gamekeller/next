.logo {
  display: inline-block;
  vertical-align: top;
  width: 32px;
  height: 32px;
  background-image: url(<%= asset_path('img/logo.svg') %>);
}

.logo-dark {
  background-image: url(<%= asset_path('img/logo-dark.svg') %>);
}

.logo-light {
  background-image: url(<%= asset_path('img/logo-light.svg') %>);
}

.logo-footer {
  background-image: url(<%= asset_path('img/logo-footer.svg') %>);
}

.logo-small {
  width: 16px;
  height: 16px;
}