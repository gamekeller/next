extends layout

block content
  table.table.table-striped.table-hover
    thead
      tr
        th ID
        th Username
        th Admin
    tbody
      each user in users
        tr
          td= user._id
          td= user.username
          td= user.admin ? 'Yes' : 'No'